# Secrets / credentials — defense-in-depth (Referee Round 4.1 S2 prerequisite)
.env
.env.*
*.env
secrets/
credentials.json
*.pem
*_api_key*
*api_key*.json

# Read-only research inputs (large, mirrored elsewhere)
research_input_data/

# Local working data, reproducible from inputs + configs
data/raw/
data/processed/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# OS / editor noise
.DS_Store
Thumbs.db
*:Zone.Identifier
.vscode/
.idea/

# Codex / Claude session artifacts
.codex/
*.codex-session
.claude/settings.local.json

# Round 4 acquired data — large vendor data, regenerable from API keys
data/acquired/*
data/processed/

# BX01 exception: commit small derived artifacts + manifest (raw artifacts stay ignored,
# their sha256 lives in manifest.csv). Per ask_0006: "Raw downloaded artifacts may
# remain gitignored if large/noisy; if so, commit checksummed manifests."
!data/acquired/bx01_kospi200_index_event_source_a0/
data/acquired/bx01_kospi200_index_event_source_a0/raw/

# User portfolio operations / NAV history — private financial data
paper_trading/operations/nav_history/
paper_trading/operations/tax_ledger/
paper_trading/operations/rebalance_reports/

# Large CSV / parquet result files (chatbots can't process; only .md / spec is shared)
# Keep small audit / metric CSV; exclude big signal / nav / sanity dumps
reports/experiments/**/signals.csv
reports/experiments/**/daily_nav.csv
reports/experiments/**/daily_nav_by_*.csv
reports/experiments/**/drift_paths.csv
reports/experiments/**/mdd_drawdown_paths.csv
reports/experiments/**/sanity_diagnostics.csv
reports/experiments/**/stage4_sanity_diagnostics.csv
reports/experiments/**/equity_curve.csv
reports/experiments/**/trades.csv
reports/experiments/**/sector_aggregate_daily_*.csv
reports/experiments/**/stock_with_sector_daily_*.csv

# Any file > 50MB (defensive — should already be covered above)
# Use git lfs separately if a large file actually needs sharing.

# Codex<->Claude tmux relay scratch (transport files; reproducible, not source)
.codex-claude-relay/
relay.out

# Bull/Bear (bb/) workflow: keep the .md role briefs/handoff/protocols versioned, but
# the runtime channels (Referee directive queues + agent reply files) are scratch.
bb/bull_inbox/
bb/bear_inbox/
bb/ask_*.md
bb/*_reply_*.md
